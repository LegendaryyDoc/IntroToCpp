#pragma once
#include <string>
using namespace std;

// Save Data
/*
struct saveData
{
	char playerName[50] = "";
	int deathCount;
	int deepestFloor;
	
	bool hasBeenBeatenGame;

	double timePlayed;
};

bool saveDataToList(string filePath, saveData saveDataArray[], int arrSize);

bool readSaveData(string fileParh);
*/

// Monster Creature Quest
	// add a monster by ID

	// remove a monster by ID

	// view a monster by ID

	// browse monsters

	// Exit