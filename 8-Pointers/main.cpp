#include<iostream>
/*
int getFirstNumber(int * arr)
{
	return arr[0];
}


// Print an Array of Floats
void printFloats(float * arr, int size)
{
	for (int i = 0; i < size; i++)
	{
		std::cout << arr[i] << std::endl;
	}
}

// Sum of an Integer Array
int arraySum(int * arr, int size)
{
	int sum = 0;

	for (int i = 0; i < size; i++)
	{
		sum = sum + arr[i];
	}
	return sum;
}


// Initializing an Array of Booleans
void initBools(bool * arr, int size, bool defaultValue)
{
	for (int i = 0; i < size; i++)
	{
		arr[i] = defaultValue;
	}
}


// Get Integer Subarray


void subarray(int * srcArr, int * dstArr, int startIdx, int endIdx)
{
	int idx = 0;

	for (int i = startIdx; i < endIdx; i++)
	{
		dstArr[idx] = srcArr[i];

	}
}
*/

// Swapping Pointers


int main()
{
	/*
	int coffeeCup = 0;
	int * pointer = &coffeeCup;

	std::cout << coffeeCup << std::endl;
	std::cout << pointer << std::endl;
	std::cout << *pointer << std::endl;

	coffeeCup = 3;
	std::cout << "Coffee is now 3" << std::endl;

	std::cout << coffeeCup << std::endl;
	std::cout << pointer << std::endl;
	std::cout << *pointer << std::endl;

	*pointer = 7;
	std::cout << "assign 7 to value pointed by pointer" << std::endl;

	std::cout << coffeeCup << std::endl;
	std::cout << pointer << std::endl;
	std::cout << *pointer << std::endl;

	int numbers[] = { 1, 2, 3, 4, 5, 6 };

	int gummyBear = 5;

	int value = getFirstNumber(&gummyBear);


	// Print an Array of Floats
	float printArr[] = { 1.2f, 6.1f, 8.3f, 12.6f, 14.4f, 15.9f };

	printFloats(printArr, 6);


	// Sum of an Integer Array
	int arr[] = {0, 1, 2, 3, 4, 5, 6};

	int val = arraySum(arr, 7);


	// Initializing an Array of Booleans
	bool arr[] = { 0, 1, 2, 3, 4, 5, 6 };

	initBools(arr, 7, true);


	// Get Integer Subarray
	int otherNumbers[] = { 4,1,2,3,3,4 };
	subarray(otherNumbers);
	*/

	// Swapping Pointers
}